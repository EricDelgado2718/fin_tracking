[pytest]
testpaths = tests
markers =
    slow: hits live Plaid sandbox or Google Sheets; skipped by default
    bask_csv: only runs if Bask is unsupported by Plaid and CSV fallback exists
addopts = -ra --strict-markers
